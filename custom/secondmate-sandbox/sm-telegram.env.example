# Sandbox secondmate — TEAM bot config. Copy to sm-telegram.env and fill in.
# NEVER commit the real file. Separate bot from firstmate's telegram.env on purpose.
SM_BOT_TOKEN=replace-with-token-from-@BotFather
SM_TEAM_GROUP_IDS=-100xxxxxxxxxx      # group id(s) the team uses, comma-separated
SM_FM_TARGET=sm-team:0                # tmux pane of the sandbox secondmate agent
SM_CAPTAIN_ID=8977397557              # captain's chat id -> admin / approve-merge authority
